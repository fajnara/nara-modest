import { categorySchema } from "./category";
import { productSchema } from "./product";
import { storeSettingsSchema } from "./storeSettings";
import { adminUserSchema } from "./adminUser";

export const schemaTypes = [categorySchema, productSchema, storeSettingsSchema, adminUserSchema];
